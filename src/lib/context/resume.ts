/**
 * RESUME CONTEXT
 *
 * Inject structured experience and achievements here.
 * Format: role, organization, period, key contributions.
 */
export function resumeContext(): string {
  return ""; // Populate in Stage 2
}
