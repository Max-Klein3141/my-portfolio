/**
 * PROJECTS CONTEXT
 *
 * Inject project descriptions here.
 * Format: name, problem solved, approach, tech, outcomes.
 */
export function projectsContext(): string {
  return ""; // Populate in Stage 2
}
