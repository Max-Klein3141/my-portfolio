/**
 * NOTES CONTEXT
 *
 * Inject raw thinking, fleeting observations, and unpolished ideas.
 * These make the AI feel more alive and authentic.
 */
export function notesContext(): string {
  return ""; // Populate in Stage 2
}
